var a = 5;
var b =  12;
function add(a, b) {
	return a + b;

}
var sum=add(a,b);
console.log(sum);

function subtract(a, b) {
	return b - a;
}
var difference = subtract(a,b);

function multiply(a, b) {
	return a * b;
}
var product = multiply(a,b);


	console.log("My football team lost " + difference + " times this week");


console.log("I CAN ADDZ " + sum + " NUMBERS");


function checkProduct(product, difference){
	return product * difference;
}

var prodiff = checkProduct(product, difference);
console.log(prodiff);

function addThenSubtract(a, b, product){
	return a + b - product;
}

function addThenMultiply(b, sum, a){
	return (b + sum) * a;
}
var howMany = addThenMultiply(b, sum, a);
console.log(howMany);

function createFullName(firstName, lastName){
	return firstName + " " + lastName;
}

var fullName = createFullName(firstName, lastName);
console.log(fullName);


function verifyDrinkingAge(age)
{
	if (age < 21){
		return false;
	}
	else {
		return true;
	}
}

console.log (verifyDrinkingAge(21));


function canDrinkBeer(age){
	if (age<21){
		console.log("The party will have tons of Cake");
	}

	else {
		console.log("The party will have an open bar");
	}
}

(canDrinkBeer(21));


function createFood(firstName, lastName, food){
	return fullName + " loves to eat " + food;
}

var eatFood = createFood("Papa", "John", "pizza");
console.log (eatFood);


var howMany = x;
function printMessage()
"Can Drink Beer"
"Let it go"

function repeater(howMany, oldEnough){
	for(var i = 0; i < howMany; i++){
		if (odlEnough){
			consol.log('yay:);
		}els{
			concole.log('boo';
		}
	}
}






